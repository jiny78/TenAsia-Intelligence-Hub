# =============================================================
# TenAsia Intelligence Hub — Docker 환경 변수 템플릿
#
# 사용법:
#   cp .env.docker .env
#   .env 파일을 열어 실제 값 입력 후 저장
#
# ⚠️  .env 는 .gitignore 에 포함되어 있으므로 커밋되지 않습니다.
# =============================================================

# ── PostgreSQL ─────────────────────────────────────────────────
POSTGRES_DB=tenasia
POSTGRES_USER=tenasia
POSTGRES_PASSWORD=change_me_in_production   # ← 반드시 변경

# ── FastAPI ────────────────────────────────────────────────────
# DATABASE_URL 은 docker-compose.yml 에서 위 PostgreSQL 변수로 자동 조합
# (컨테이너 내부 호스트명 'db' 사용)

GEMINI_API_KEY=                             # ← Google AI Studio 에서 발급

# AWS (S3 썸네일 저장, Secrets Manager 등)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=tenasia-thumbnails

# ── Next.js (브라우저가 직접 호출하는 URL) ────────────────────
# 개발: 브라우저 → host의 8000 포트로 직접 접근
NEXT_PUBLIC_API_URL=http://localhost:8000

# 프로덕션 서버 배포 시 도메인으로 변경:
# NEXT_PUBLIC_API_URL=https://api.your-domain.com

# ── 포트 오버라이드 (선택, 기본값 사용 시 주석 유지) ─────────
# API_PORT=8000
# WEB_PORT=3000
# DB_PORT=5432
