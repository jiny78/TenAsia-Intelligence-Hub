# =============================================================
# TenAsia Intelligence Hub — .gitignore
# 보안·시스템 의존 파일 완전 격리
# =============================================================

# ─────────────────────────────────────────────────────────────
# ❌ 보안 — 절대 커밋 금지
# ─────────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
.env.production
.env.staging
.env.development
.env.test
.env.ci
.env.override
# .env.example 은 커밋 허용 (값 없는 템플릿)
!.env.example

# AWS 자격증명
.aws/
*.pem
*.key
*_rsa
*_rsa.pub
*_ed25519
*_ed25519.pub

# Google 서비스 계정 / OAuth 자격증명
credentials.json
service_account*.json
client_secret*.json
token.json

# 시크릿 파일 일반
*.secret
secrets.json
gitleaks-report.json
trufflehog-report.json

# Terraform 민감 파일
terraform/terraform.tfvars
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/tfplan
terraform/crash.log
terraform/crash.*.log

# ─────────────────────────────────────────────────────────────
# 🐍 Python
# ─────────────────────────────────────────────────────────────
__pycache__/
**/__pycache__/
*.py[cod]
*$py.class
*.so

# 패키지 빌드
build/
dist/
*.egg-info/
.eggs/
*.egg
MANIFEST

# 배포 도구
pip-log.txt
pip-delete-this-directory.txt

# 가상 환경
.venv/
venv/
ENV/
env/
.env/
env.bak/
venv.bak/

# Jupyter
.ipynb_checkpoints
*.ipynb_checkpoints/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# pytype
.pytype/

# Cython
cython_debug/

# ─────────────────────────────────────────────────────────────
# 🧪 테스트 & 커버리지
# ─────────────────────────────────────────────────────────────
.pytest_cache/
.tox/
.nox/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
*.py,cover
nosetests.xml
test-results/
junit-*.xml

# ─────────────────────────────────────────────────────────────
# 📋 로그 (logs/ 디렉터리 내용물만 제외, .gitkeep 유지)
# ─────────────────────────────────────────────────────────────
logs/*
!logs/.gitkeep
*.log
*.log.*
log/
*.out

# ─────────────────────────────────────────────────────────────
# 🗄 데이터베이스
# ─────────────────────────────────────────────────────────────
# 로컬 DB 파일
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-shm
*.db-wal

# Alembic 임시
alembic.ini.bak
database/*.db

# ─────────────────────────────────────────────────────────────
# 🐳 Docker
# ─────────────────────────────────────────────────────────────
.docker/

# ─────────────────────────────────────────────────────────────
# ☁️ Streamlit
# ─────────────────────────────────────────────────────────────
.streamlit/secrets.toml
.streamlit/config.toml

# ─────────────────────────────────────────────────────────────
# 💻 IDE & 에디터
# ─────────────────────────────────────────────────────────────
# VS Code
.vscode/
!.vscode/extensions.json
!.vscode/settings.json.example

# JetBrains (PyCharm, IntelliJ)
.idea/
*.iml
*.iws
*.ipr
.idea_modules/

# Vim / Emacs
*.swp
*.swo
*~
\#*\#
.\#*

# Sublime Text
*.sublime-workspace
*.sublime-project

# ─────────────────────────────────────────────────────────────
# 🖥 운영체제
# ─────────────────────────────────────────────────────────────
# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
.AppleDouble
.LSOverride
Icon?

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~

# ─────────────────────────────────────────────────────────────
# 📦 임시 & 빌드 산출물
# ─────────────────────────────────────────────────────────────
*.bak
*.tmp
*.temp
*.orig
*.rej
*.patch
tmp/
temp/

# 미디어 임시 파일 (스크래퍼가 생성하는 임시 다운로드)
/tmp_downloads/
*.download

# ─────────────────────────────────────────────────────────────
# 🔧 기타 도구
# ─────────────────────────────────────────────────────────────
# pre-commit
.pre-commit-config.yaml.bak

# profiling
*.prof
*.lprof

# rope (Python 리팩토링)
.ropeproject
